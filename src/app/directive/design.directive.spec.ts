import { DesignDirective } from './design.directive';

describe('DesignDirective', () => {
  it('should create an instance', () => {
    const directive = new DesignDirective();
    expect(directive).toBeTruthy();
  });
});
